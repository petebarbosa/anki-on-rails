<%= render 'home/header' %>

<%= turbo_frame_tag @page do %>
  <hero class="flex flex-col place-items-center">
   <h1 class="font-bold text-3xl mt-20 mb-8">Anki on Rails</h1>
   <h2 class="font-semibold text-6xl text-center"><%= t("home.hero_top") %></h2>
   <p class="text-2xl my-8 text-center"><%= t("home.hero_bottom") %></p>
   <%= link_to 'Get Started', new_user_session_path, class: 'inline-flex items-center px-6 py-3 bg-blue-600 text-white font-semibold rounded-xl hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition duration-150 ease-in-out' %>
  </hero>
<% end %>
<%= render 'shared/footer' %>
